apiVersion: v1
kind: Service
metadata:
  name: tech-service
spec:
  
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 443
      nodePort: 30000
  type: NodePort
